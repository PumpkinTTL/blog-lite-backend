node_modules/
dist/
coverage/
*.log
*.local

# Environment variables
.env
.env.*
!.env.example

# Uploads (local storage)
api/uploads/

# Docs (reference only)
admin/src/docs/

# 本地调试用测试页（不跟踪，留磁盘自用）
admin/src/views/ScrollTestView.vue

# Windows reserved device name accidentally created by `> nul` redirections in git-bash
nul
